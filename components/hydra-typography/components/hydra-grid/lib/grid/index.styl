// Import from installed modules
@import "../../components/stylus-type-utils"

// Import the grid generator
@import "core"
@import "grid"

grid-generate($units, $prefix = "")
  grid-generate-core(mixin == 'root')
  grid-generate-columns($units, $prefix, mixin == 'root')
